SELECT CAST(TPEP_PICKUP_DATETIME AS date) AS DIA,
	MAX(TIP_AMOUNT)
FROM YELLOW_TAXI_TRIPS
WHERE CAST(TPEP_PICKUP_DATETIME AS date) in ('20210120',
											'20210104',
											'20210101',
											'20210121')
GROUP BY CAST(TPEP_PICKUP_DATETIME AS date)
order by 2 desc
limit 1